name: syncedu_local
description: Local SQLite mirror, outbox and sync engine for SyncEdu.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

resolution: workspace
dependencies:
  connectivity_plus: ^7.3.1
  drift: ^2.34.4
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  syncedu_core:
    path: ../syncedu_core
  uuid: ^4.6.0
dev_dependencies:
  build_runner: ^2.15.1
  drift_dev: ^2.34.0
  flutter_test:
    sdk: flutter
